apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-devops-demo

spec:
  type: {{ .Values.service.type }}

  selector:
    app: devops-demo

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}